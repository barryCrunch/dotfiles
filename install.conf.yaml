- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.config/nvim/init.vim: init.vim
    ~/.bashrc: bashrc

- create:
    - ~/downloads

- shell:
  - [git submodule update --init --recursive, Installing submodules]
